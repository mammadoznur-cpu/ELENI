(async()=>{const g=document.querySelector('[data-gallery]');if(!g)return;try{const r=await fetch('/data/gallery.json',{cache:'no-store'});const d=await r.json();g.innerHTML='';(d.images||[]).forEach((x,i)=>{const f=document.createElement('figure');f.innerHTML=`<img src="${x.src}" alt="${x.title||`ELENİ ${i+1}`}"><figcaption>${x.title||`Görsel ${String(i+1).padStart(2,'0')}`}</figcaption>`;g.appendChild(f)})}catch{g.innerHTML='<p>Galeri yüklenemedi.</p>'}})();
